/**
 * Module Dependencies
 */
var scripts = require('../');

/**
 * Setup
 */

var opts = {
  port: 8000
};

// var callback = function(){};

scripts.lift();
// scripts.lift(opts);
// scripts.lift(callback);
// scripts.lift(opts, callback);

